# TreeFormatters

`TreeFormatter` and `TreeFormatterBuilder` are used to define a custom string representation for an entire `DerivationTree`.

## Building a Formatter

You can define different formatting rules for different types of nodes (rules, terminals, errors).

```java
// Example of TreeFormatterBuilder
```
