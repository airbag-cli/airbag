# Derivation Trees

The `DerivationTree` is the core data structure representing the parsed input. It's a tree of nodes that can be either rules or terminals.

## Structure

[Explanation of Rule, Terminal, and Error nodes]
